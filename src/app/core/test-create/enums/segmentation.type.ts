export enum SegmentationTypes {
  Custom = 'BIC.Use my own consumer segments',
  Predefined = 'BIC.Use InsightMe predefined consumer segments',
  NoSegments = 'BIC.No specific consumer segment',
}

export enum SegmentationTypesWithoutNot {
  Custom = 0,
  Predefined = 1,
}
